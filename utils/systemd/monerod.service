[Unit]
Description=Monero Full Node
After=network.target

[Service]
User=monero
Group=monero
WorkingDirectory=~
RuntimeDirectory=monero

Type=forking
PIDFile=/run/monero/golderod.pid

ExecStart=/usr/bin/golderod --config-file /etc/monerod.conf \
    --detach --pidfile /run/monero/monerod.pid

Restart=always
PrivateTmp=true

[Install]
WantedBy=multi-user.target
